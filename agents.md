To build the project, use 'make build_Release' command.

Do not make comments descripting changes from previous version, like "Sys::SleepMilliseconds removed". Make comments descripting current version of code only.
